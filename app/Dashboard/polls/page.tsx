export const dynamic = "force-dynamic";
export const revalidate = 0;

import PollsComponent from "@/components/dashboard/PollsComponent";

export default function PollsPage() {
  return <PollsComponent />;
}
